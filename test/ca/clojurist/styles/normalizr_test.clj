(ns ca.clojurist.styles.normalizr-test
  (:require [clojure.test :refer :all]
            [ca.clojurist.styles.normalizr :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
